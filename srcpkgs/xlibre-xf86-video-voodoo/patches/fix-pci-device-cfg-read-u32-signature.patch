--- a/src/voodoo_hardware.c
+++ b/src/voodoo_hardware.c
@@ -134,7 +134,7 @@ pci_enable(VoodooPtr pVoo)
     }
 
     if (PCI_MAP_IS64BITREG(x)) {
-        CARD32 x;
+        uint32_t x;
         PCI_READ_LONG(pVoo->PciInfo, &x, 0x40);
         baseaddr |= (uint64_t)x << 32;
     }
